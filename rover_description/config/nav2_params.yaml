map_server:
  ros__parameters:
    yaml_filename: "~/map.yaml"

amcl:
  ros__parameters:
    initial_pose:
      x: 0.0
      y: 0.0
      theta: 0.0

controller_server:
  ros__parameters:
    base_frame_id: "base_link"

planner_server:
  ros__parameters:
    planner_plugins: ["GridBased"]